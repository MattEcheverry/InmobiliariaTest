## Ajustes rapidos de contenido (UI pass v101)

- WhatsApp:
  - Link y numero: `app.js` en `DEFAULT_WHATSAPP`.
  - Mensaje por defecto: parametro `text=` dentro de `DEFAULT_WHATSAPP`.
- Contacto y direccion:
  - Editar en `about.html` (actualizar).
  - Editar en `clients.html` (actualizar).
  - Si existe bloque de contacto en footer HTML, ajustar esos textos (actualizar).

